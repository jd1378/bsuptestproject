// saving all endpoints in one place, so can change them easily if needed
const urls = {
  baseURL: "http://hiring.bsup.tk/api",
  books: "/books",
  user: "/user",
  logout: "/logout"
};

export default urls;
